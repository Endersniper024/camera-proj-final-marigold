# Tools

No standalone interactive annotation tool is required for this Marigold submission. The manually annotated challenge pairs are included in `../data/annotations/web_depth_pairs.csv`, and the corresponding evaluator is `../scripts/remote/web_whdr.py`.
